# 窃语黑市 v0.1.0 安装说明

这是可在 Codex 中玩的中文单人游戏包，包含第一章《失窃的账本》。不包含模型、账户、API Key 或作者的存档。请把 ZIP 整包解压，不要只拖出一个脚本。

## 准备

- 已登录且有可用模型额度的 Codex 桌面应用或 CLI。
- Python 3.11 或以上，负责本地规则和存档，不需安装 Python 第三方库。
- 自动安装器需要 Codex CLI 的 `plugin` 命令。仅装了桌面应用却没有该命令时，可让 Codex 按下面的方法安装，不必自行操作终端。

这意味着第一版的运行条件是 **Codex + 可用模型额度 + Python**，并非在所有只安装 Codex 的电脑上零依赖启动。游戏内金币是虚构资源；游玩消耗玩家自己的 Codex 使用额度。无需提供或购买作者的模型密钥。

## 推荐：让 Codex 安装

把解压后的文件夹添加为 Codex 项目，在对话里说：

> 请读取这个分享包的安装说明，为我安装窃语黑市插件。检查 Python 和 Codex 插件能力。安装后告诉我如何开始，不要公开上传，也不要改我的全局模型。

给安装助手的具体说明：

1. 先用 `python install.py --prepare-only` 校验并复制到用户主目录下的 `Games/WhisperMarket/0.1.0`，取得输出的真实路径。如果已有不同内容，会新建旁边的目录，保留旧文件。
2. 有 Codex CLI 时执行 `python install.py`。它通过 `codex plugin marketplace add <已复制目录>` 注册本分享包自带的本地市场，再执行 `codex plugin add whisper-market@whisper-market-share`。不修改其他插件，不改变模型，不读取登录凭证。
3. 如果桌面环境没有 CLI，使用 Codex 自带的 `plugin-creator` 技能按个人插件流程创建 `whisper-market`，再将本包 `plugins/whisper-market` 的内容复制进返回的插件目录，保留规范的个人市场入口。在插件界面打开并安装；不要凭空声称安装成功。若桌面版本没有本地插件安装能力，更新官方 Codex 后再试。
4. 成功后打开**新的 Codex 对话**，说“开始玩窃语黑市”。当前安装对话不会自动获得新技能。

## 自己安装

Windows 可双击 `Windows安装.cmd`。它使用电脑已安装的 Python 和 Codex CLI；失败会保留窗口，不会自动下载软件。

macOS / Linux 或 Windows 终端可在本包目录运行：

```text
python3 install.py
```

Windows 常用 `python install.py`。新对话使用“开始玩窃语黑市”。标准推荐 **GPT-5.6 Sol · Medium**；可选择自己账户里可用的其他 GPT 模型。推荐不代表已经完成不同模型的盲测。

## 分享、存档和更新

- 直接分享原始 ZIP；不要分享用户数据目录中的存档和笔记。`SHA256.json` 用于核对文件完整性，不是作者数字签名。
- Windows 默认存档：`%LOCALAPPDATA%/WhisperMarket/saves.sqlite3`。macOS / Linux：`~/.local/share/WhisperMarket/saves.sqlite3`。面板在旁边的 `panels` 目录。
- 游戏内每次行动自动保存；说“继续窃语黑市”即可读取。说“重新开始”会另开存档，不覆盖旧局。
- 安装器遇到内容不同的旧目录会保留它。升级版本请保持存档目录不动，按新包说明操作。本版本尚未承诺未来存档格式的迁移兼容。
- 本包包含三个生成场景插画、规则程序与游戏提示词。尚未上架 Codex 官方插件目录；转售或另外发布素材的授权没有在本版本中设定。
